# VantageTube AI — Python Dependencies

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database
supabase==2.7.4

# AI
google-generativeai==0.7.2

# HTTP client
httpx==0.27.2

# Auth
python-jose[cryptography]==3.3.0
bcrypt>=4.0                     # used directly in security.py (passlib removed — incompatible with bcrypt 4+)

# Config / Validation
python-dotenv==1.0.1
pydantic==2.8.2
pydantic[email]==2.8.2          # installs email-validator for EmailStr fields
pydantic-settings==2.4.0
email-validator>=2.0            # explicit — required by EmailStr in schemas/auth.py

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
